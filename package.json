{
    "name": "workspace-root",
    "version": "1.0.0",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "prettier": "prettier --write .",
        "build": "pnpm -r build"
    },
    "devDependencies": {
        "prettier": "^3.2.5"
    }
}
